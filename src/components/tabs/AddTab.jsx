import React from 'react';

const AddTab = ({ onSubmit, categories }) => {
  return (
    <div style={{ padding: '1.5rem', textAlign: 'center' }}>
      <h2>➕ Add Tab</h2>
      <p style={{ color: 'var(--text-secondary)', marginTop: '1rem' }}>
        Em construção... (Fase 3)
      </p>
    </div>
  );
};

export default AddTab;
