# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# Finnhub.io API key — free at https://finnhub.io
# Enables live stock price quotes in the Patrimony tab.
# Leave empty to disable (app falls back to stored prices).
VITE_FINNHUB_KEY=
